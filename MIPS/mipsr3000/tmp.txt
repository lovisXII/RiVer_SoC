GIT ADD:

this
execute.h
mux_execute.h


questions 

- execute.h

status register:
    registre du coprocesseur si on est en mode user ou system
swap instruction:
    a supprimer
redopc write enable => redopc?:

execute process YOPER et XOPER 
copying capability:

- mux_execute.h

bubble, keep and hold
stall rappel
diff between ovf_re and ovf_xe
diff between RES_SE and RES_RE (managed by keep signal)
diff between COPYCAP_SE and COPYCAP_RE
sw exec (break) or (syscall)
syscall : appel sys 
break: utiliser pour debugger mais meme system 
x: exception ou interruption